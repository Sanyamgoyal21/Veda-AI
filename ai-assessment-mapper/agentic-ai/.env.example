PORT=8000

# Vision-capable AI provider credentials.
# Never expose these values to the frontend or commit real values.
AI_API_KEY=your_openai_api_key_here
AI_MODEL=gpt-4o
# Optional: override the API base URL to point at an OpenAI-compatible
# provider (e.g. Gemini's endpoint) instead of OpenAI itself. Leave unset
# for OpenAI.
# AI_BASE_URL=https://generativelanguage.googleapis.com/v1beta/openai/

# Max image dimension (px) sent to the vision model, larger pages are downscaled.
MAX_IMAGE_DIMENSION=1600

# Render DPI used when rasterizing PDF pages to images.
PDF_RENDER_DPI=200
